# CARRIL C — SLICE 3b (GASTOS: CARGA A11 + LISTADO A13) — CIERRE

**Estado:** ✅ **Cerrado y verificado en TEST.** Slice 3b sumó el **gasto interno** al Portal Operativo Interno con dos acciones companion sobre `gastos_internos`: **A11** (`cargar.gasto_interno`, la **primera escritura no-idempotente** del portal, con guard de **dos capas** anti-replay + idempotencia de negocio) y **A13** (`gastos.listado`, su lectura companion por período contable). Cada acción quedó cableada por **los dos caminos** — wrapper n8n directo **y** gateway `portal-api` por JWT —, con cuatro mini-cierres ya emitidos. La infra de idempotencia (`portal_idempotencia`, `portal_cargar_gasto_interno`) vive **fuera del canónico**, TEST-only. **Cierra P-C-9 en TEST para Carril C.**

**Entorno de validación:** TEST (`vita-delta-test`, ref `bdskhhbmcksskkzqkcdp`) — IDs de cabaña 1–5, marcador `configuracion_general.ambiente='test'`.
**Entorno de operación:** — (Slice 3b **no** se promovió a OPS; el Carril C no toca OPS para experimentar).
**Fecha de cierre:** 2026-06-22.
**Base:** `C_SLICE3A_CIERRE.md` (lecturas A24/A25, molde de wrapper de lectura, D-C-52…54, L-C-18/19) + `C_SLICE2_A10_DIRECTO_CIERRE.md` (espina de la primera escritura: estructura transaccional con lock, modelo de error de dos capas, teardown FK-safe, D-C-50/51) + los **cuatro mini-cierres del slice**: `C_SLICE3B_A11_DIRECTO_CIERRE.md` / `C_SLICE3B_A11_GW_CIERRE.md` / `C_SLICE3B_A13_DIRECTO_CIERRE.md` / `C_SLICE3B_A13_GW_CIERRE.md` + snapshot anti-OPS `C_SLICE3B_SNAPSHOT_A11_A13.sql`.
**Depende de:** D-C-29 (HMAC sobre bytes literales + `ts` ±300 s + `nonce` firmado; sin store persistente hasta A11/P-C-9) · D-C-34 (infra del portal fuera del canónico, REVOKE a los 4 roles, invisible al Data API; precedente `portal_usuarios`) · D-C-39/41 (allowlist doble + action binding) · D-C-43/47 (contrato JSON `{ok,data|error}`, lista vacía → `ok:true`) · D-C-50/51 (`isWrite` → `estado_incierto` ante dispatch no confiable; modelo de error de dos capas) · D-C-03 (jenny sin contenido económico) · **D-NEG-01/02** (25 % operativo; inicio contable 2026-07-01 = floor duro) · L-C-05 (HMAC sobre raw body binario) · L-C-10/11 (placeholder de secreto por prefijo; TDZ del validador antes del CATALOG) · L-C-16 (`$1::jsonb` casteo del queryReplacement) · L-C-17 (cuarteto PowerShell 5.1) · L-C-19 (BIGINT → number) · L-8D-01 (leer `item.resultado.ok`) · L-9F-03 (DROP+CREATE vs `OR REPLACE`, evita auto-RLS).
**Schema canónico de referencia:** `6B_SCHEMA_SQL.md v1.8.1` — **NO modificado por este cierre.** A13 es `SELECT` inline; A11 **no** introduce DDL ni columnas en tablas canónicas (`gastos_internos` se usa tal cual: 17 columnas, 18 constraints). La infra de idempotencia (`portal_idempotencia` + `portal_cargar_gasto_interno`) es una tabla/función **nuevas FUERA del canónico**, TEST-only, no promovidas a OPS (precedente `portal_usuarios`). `gastos_internos` **no** tiene columna `source_event`: la traza/`source_event` vive en `portal_idempotencia`.
**Autores:** Franco (titular, ejecutor de **todos** los writes/deploys/imports/smokes en Supabase y n8n) + Claude (arquitecto/copiloto; estrictamente advisory — no tocó infraestructura).
**Decisiones registradas:** D-C-55, D-C-56, D-C-57, D-C-58, D-C-59, D-C-60 🔒. **Lecciones:** L-C-20, L-C-21, L-C-22, L-C-23.

---

## 1. Resumen ejecutivo

Slice 3a dio lecturas nuevas; Slice 3b da el **gasto interno** completo: una **escritura** (A11) y su **lectura** (A13), las dos sobre `gastos_internos`, cada una por wrapper directo **y** por gateway. Es el slice que materializa la **primera escritura no-idempotente** del portal y, con ella, **P-C-9** (store anti-replay de `nonce`).

**A11 `cargar.gasto_interno`** carga un gasto detrás de un guard de **dos capas en UNA transacción** dentro de `portal_cargar_gasto_interno`: **capa 1** anti-replay de `nonce` (`UNIQUE(nonce)`, el `nonce` se **deriva de la firma HMAC** del sobre — D-C-56, nunca lo manda el cliente) y **capa 2** idempotencia de negocio (`UNIQUE(action,idempotency_key)` con comparación de `payload_norm`+`actor`: mismo → `ok` idempotente; payload distinto → `payload_mismatch`; actor distinto → `actor_mismatch`). El `actor` y el `source_event` los **deriva la función** server-side; las **18 constraints** de `gastos_internos` son el gate de coherencia. La infra (`portal_idempotencia`, función) vive **fuera del canónico** (precedente `portal_usuarios`, D-C-34). En el gateway, el `idempotency_key` viaja **sibling de `payload`** y se firma **top-level** vía un param **opcional aditivo** que deja byte-idénticos los sobres de las 11 acciones previas (D-C-57).

**A13 `gastos.listado`** es la lectura companion por **período contable**, patrón Slice 3a (`SELECT` inline, universo filtrado completo en SQL, agregados en centavos y paginación en el render), **sin** `injectActor` ni `isWrite`. Los bordes de período se **truncan a primer día de mes** antes del clamp y del check (D-C-59), con `periodo_hasta` híbrido. El gateway A13 **espeja esa semántica mensual** (D-C-60) e incluye la precisión `null == omitido` (paridad con el wrapper directo; A25 mantiene su semántica estricta de `null`, D-C-54 no se reabre).

La validación fue **empírica y por capa**, los cuatro caminos: A11 directo (seguridad con `secWrites=0` + funcional con replay/retry/doble-click/conflictos + teardown FK-safe) y gateway (spoof de control en payload y top-level + regresión `conflicto`≠`estado_incierto`); A13 directo y gateway con **cruce al centavo** contra el ground-truth. Metodología estricta respetada (snapshot read-only → blueprint en chat → diff mínimo → Franco ejecuta → verificación). **OPS y `6B_SCHEMA_SQL.md` intactos.**

---

## 2. Qué quedó construido en TEST

El gateway `portal-api` quedó con **13 entradas** en el CATALOG: 1 resuelta en la Edge (`sesion.contexto`, A02) + **8 lecturas n8n** (5 de Slice 1 + A24/A25 de Slice 3a + **A13** de este slice) + **4 escrituras n8n** (3 de Slice 2 + **A11** de este slice).

### 2.1 A11 `cargar.gasto_interno` — carga de gasto interno (primera escritura no-idempotente)
- Roles {vicky, socio}; **jenny excluida** (D-C-03). Wrapper `portal-a11-cargar-gasto-interno__TEST` (con `__TEST`: convención de **escrituras** A07/A08). Gateway: `injectActor:true`, `isWrite:true`, `needsIdempotencyKey:true`.
- **Payload de negocio (13 claves, REJECT-UNKNOWN):** `fecha`, `periodo?` (día 1; default = primer día del mes de `fecha`), `clase` (`A`/`C`/`D`/`E`), `clase_sugerida?`, `etiqueta`, `monto` (>0 → `numeric(12,2)`), `id_zona?`, `id_cabana?`, `pagador_tipo` (`socio`/`caja`), `id_socio_pagador?`, `medio_pago?`, `comentario?`, `comprobante_url?`. **Control** (`actor`/`rol`/`nonce`/`request_ts`) lo inyecta el wrapper/gateway, **nunca** el cliente.
- **Guard de dos capas en UNA transacción** dentro de `portal_cargar_gasto_interno` (Camino B — función infra): `pg_advisory_xact_lock(hashtext(action), hashtext(idempotency_key))` → anti-replay de `nonce` → idempotencia `(action,idempotency_key)` → alta nueva (INSERT gasto + INSERT traza en el mismo sub-bloque con savepoint; constraint → `payload_invalido` con `detail.constraint`, revierte ambos, sin huérfano). **18 constraints** de `gastos_internos` = gate de coherencia.
- **Infra TEST-only fuera del canónico:** `public.portal_idempotencia` (12 columnas; PK + 2 UNIQUE —`nonce`, `(action,idempotency_key)`— + 1 FK RESTRICT a `gastos_internos` + 7 CHECK; RLS off) y `public.portal_cargar_gasto_interno(jsonb) SECURITY INVOKER`. `gastos_internos` **no** lleva `source_event`.

### 2.2 A13 `gastos.listado` — listado de gastos por período contable (lectura companion)
- Roles {vicky, socio}; **jenny excluida** (D-C-03). Wrapper `portal-a13-gastos-listado` (**sin `__TEST`**: convención de **lecturas** A12/A24/A25). **Lectura `SELECT` inline**, **sin** `injectActor` ni `isWrite`.
- **Filtros opcionales (REJECT-UNKNOWN; ausente/null = sin filtro):** `periodo_desde`/`periodo_hasta` (truncados a primer día de mes, D-C-59; floor duro 2026-07-01 clampea `periodo_desde`; `periodo_hasta` híbrido), `clase` (`A`/`C`/`D`/`E`), `id_zona`/`id_cabana` (entero>0), `pagador_tipo` (`socio`/`caja`), `q` (string trim 1..120, ILIKE sobre `etiqueta`+`comentario`, parametrizado), `limit` (1–200, default 50), `offset` (default 0).
- **Agregados sobre el universo filtrado completo** (no la página): `total_gastos = SUM(monto)` en **centavos**, `por_clase = [{clase, monto, n}]`. Contrato `data:{ periodo_desde, periodo_hasta, total_gastos, por_clase, filas, limit, offset }`; vacío → `total_gastos:0, por_clase:[], filas:[]`, `ok:true`. **20 columnas/fila** (incluye `moneda` ARS por default), IDs BIGINT → número, `socio_pagador_nombre`/`zona`/`cabana` por LEFT JOIN.

---

## 3. Acciones ejecutadas (bitácora)

| Acción | Wrapper | Tipo | injectActor / isWrite / needsIdemKey | Estado |
|---|---|---|:---:|---|
| **A11** `cargar.gasto_interno` | `portal-a11-cargar-gasto-interno__TEST` | escritura (función infra) | ✓ / ✓ / ✓ | cerrado (directo 38/38 + gateway 24/24 + teardown PASS) |
| **A13** `gastos.listado` | `portal-a13-gastos-listado` | lectura (SELECT inline) | — / — / — | cerrado (directo 32/32 + gateway 34/34, cruce al centavo) |

---

## 4. Decisiones registradas (D-C-55, D-C-56, D-C-57, D-C-58, D-C-59, D-C-60) 🔒

> Introducidas/aplicadas en este slice; este cierre las **promueve** a `DECISIONES_NO_REABRIR.md`.

- **D-C-55 — Estructura atómica de la carga de gasto (A11), guard de dos capas.** A11 cumple **P-C-9** con un guard de **dos capas en UNA transacción**, todo dentro de `portal_cargar_gasto_interno` (Camino B — función infra; el INSERT inline se descartó para separar guard de inserción): **Capa 1 — anti-replay de `nonce`** (`UNIQUE(nonce)`): un sobre firmado ya visto → `conflicto`/`nonce_replay`. **Capa 2 — idempotencia de negocio** (`UNIQUE(action,idempotency_key)`) con comparación de `payload_norm` (jsonb normalizado, `IS DISTINCT FROM`) **y** `actor`: misma key + mismo payload + mismo actor → `ok` idempotente (devuelve el `id_gasto` existente); payload distinto → `payload_mismatch`; actor distinto → `actor_mismatch`. Secuencia: `pg_advisory_xact_lock(hashtext(action), hashtext(idempotency_key))` → capa 1 → capa 2 → alta nueva (INSERT gasto + INSERT traza en el **mismo sub-bloque con savepoint**; constraint → `payload_invalido` con `detail.constraint`, revierte ambos, **sin huérfano**). **`source_event = 'portal_a11_' || idempotency_key` y `creado_por = actor` los DERIVA la función**, nunca el cliente (un `creado_por` en el payload → `payload_invalido` por REJECT-UNKNOWN). **`portal_idempotencia` es infra del portal FUERA del canónico** (precedente `portal_usuarios`, D-C-34): store anti-replay + store de idempotencia + traza; `gastos_internos` **no** lleva `source_event`. Las **18 constraints de `gastos_internos`** son el gate de coherencia autoritativo.
- **D-C-56 — `nonce` = firma HMAC del sobre.** El cliente/frontend **nunca** manda `nonce`. El wrapper lo **deriva de la firma HMAC del sobre** que ya valida: mismo sobre byte-idéntico → misma firma → mismo `nonce` → `conflicto`/`nonce_replay`; retry re-firmado (otro `ts`) o doble-click lógico con la **misma** `idempotency_key` → `nonce` distinto pero respuesta **idempotente** con el mismo `id_gasto`. `nonce = expectedSignatureHex.toLowerCase()` desde la firma esperada **recomputada y normalizada**, no del header crudo; `idempotency_key` con `^[A-Za-z0-9_-]{8,64}$`. **`buildSignedEnvelope` no se toca** — la firma ya existe en ambos caminos (smoke en directo, `portal-api` en gateway), así que el gateway **no** genera `nonce`.
- **D-C-57 — `idempotency_key` top-level en el sobre del gateway, vía param opcional aditivo.** El wrapper A11 (inmutable) lee `body.idempotency_key` **top-level** y reject-unknownea control dentro del payload; el `nonce` lo deriva de la firma (D-C-56). `buildSignedEnvelope`/`dispatchN8n` reciben un param **opcional** `idempotencyKey?: string` que emite `idempotency_key` top-level **solo si está presente** → para los 11 reads/writes previos el sobre queda **byte-idéntico** (probado fijando `Date.now`/`randomUUID` y comparando). El `idempotency_key` viaja **sibling de `payload`** (frontend→gateway), validado fail-fast con `^[A-Za-z0-9_-]{8,64}$` (gobernado por `needsIdempotencyKey`). **D-C-56 sin cambios** (el `nonce` genérico sigue inerte para A11). El frontend **no** puede inyectar control ni en el payload ni top-level del request (guard global del handler + rechazo del validador) → `payload_invalido`.
- **D-C-58 — Contrato de A13 `gastos.listado` (lectura).** Acción de lectura de `gastos_internos` por **período contable**, patrón Slice 3a (universo filtrado en SQL, agregados+paginación en el render). Webhook `portal-a13-gastos-listado` **sin `__TEST`** (convención de lecturas A12/A24/A25; las escrituras llevan `__TEST`). Roles `vicky`/`socio` (sin jenny, D-C-03). Filtros **REJECT-UNKNOWN, todos opcionales, ausente/null = sin filtro**: `periodo_desde`/`periodo_hasta`, `clase {A,C,D,E}`, `id_zona`/`id_cabana` (entero seguro > 0), `pagador_tipo {socio,caja}`, `q` (string `trim` 1..120, ILIKE sobre `etiqueta`+`comentario`, parametrizado, comodín `%`/`_` sin escape en MVP), `limit` (default 50/cap 200), `offset` (default 0). **Agregados sobre el universo filtrado completo** (no la página): `total_gastos = SUM(monto)` en centavos y `por_clase = [{clase, monto, n}]`. Salida `{ ok:true, data:{ periodo_desde, periodo_hasta, total_gastos, por_clase, filas, limit, offset } }`; vacío → `total_gastos:0, por_clase:[], filas:[]`, `ok:true`. **20 columnas por fila** (incluye `moneda` ARS por default), IDs BIGINT → número, opcionales null-safe, `socio_pagador_nombre`/`zona`/`cabana` por LEFT JOIN. A13 es **lectura pura**: cero DDL/función/columna.
- **D-C-59 — Período contable: bordes truncados a primer día de mes (inclusivo por mes).** `periodo_desde` y `periodo_hasta` se **normalizan a `YYYY-MM-01`** antes del clamp y de la comparación: un borde a mitad de mes **incluye el mes completo**. Floor duro `2026-07-01` (D-NEG-02) clampea el borde inferior (clamp, no reject; el SQL lo refuerza con `GREATEST`). `periodo_hasta` **híbrido**: omitido → mes actual (sin check de inversión → con floor futuro, el `{}` por defecto da vacío `ok:true`); explícito → YMD válido y, **a nivel mes** y tras el clamp, `>= periodo_desde`, sino `payload_invalido`. Aplica a toda lectura futura cuyo eje sea `periodo` contable.
- **D-C-60 — El gateway A13 espeja la semántica MENSUAL del wrapper directo (incluye la paridad `null == omitido`).** `payloadGastosListado` trunca `periodo_desde`/`periodo_hasta` a primer día de mes (`firstOfMonth_GW(s)=s.slice(0,8)+'01'`) **antes** del clamp al floor (`2026-07-01`, D-NEG-02) y del check de inversión explícita, para **no ser más estricto que el wrapper**: un rango del mismo mes con día de hasta < día de desde **no** es inversión a nivel mes y **no** debe rebotar (el wrapper directo lo acepta y devuelve ese mes). `periodo_hasta` **híbrido**: omitido (`undefined`) **o** `null` → **no** se incluye en `value` (**paridad con el wrapper directo**, que trata `null` como omitido y defaultea al mes actual sin check); `string` → YMD válido, truncado y, tras el clamp, `>= periodo_desde` a nivel mes; **otro tipo** o mal formado/invertido → `payload_invalido`. El wrapper **re-trunca** (idempotente) y mantiene la verdad autoritativa. La **verdad de referencia de A13 es su wrapper directo**, distinta de A25 (cuyo `payloadIngresosPeriodo` rechaza `null` y queda **intacto**: divergencia legítima entre acciones distintas — D-C-54 **no se reabre**). La parte de `null == omitido` es una **precisión de paridad** de A13, no una decisión separada. Verificado por test de paridad ejecutable gateway↔wrapper (**24/24**) y por el smoke JWT (**GP** mismo-mes + **P-null** null==omitido).

---

## 5. Lecciones registradas (L-C-20, L-C-21, L-C-22, L-C-23)

- **L-C-20 — En un hit idempotente el `nonce` nuevo NO se persiste; cobertura del store de `nonce`.** Cuando la capa 2 resuelve un **hit idempotente** (misma `idempotency_key`, `payload_norm`+`actor` coincidentes), la función **devuelve el `id_gasto` existente sin INSERT** — por lo tanto el `nonce` de ese request **no** entra a `portal_idempotencia`. Implicación para auditar **P-C-9**: el store de `nonce` solo contiene los `nonce` de **altas nuevas**; un replay del sobre de un **retry** lo atrapa la **idempotencia de negocio**, no el `nonce_replay` (que solo cubre el reenvío exacto del sobre cuyo `nonce` sí se persistió, el del alta). Ambas capas convergen en la misma semántica externa (reenviar no duplica), pero no son intercambiables: la capa 1 protege contra el replay del mismo sobre; la capa 2, contra reintentos legítimos re-firmados.
- **L-C-21 — En TS strict, narrowing de `unknown` a string validado requiere type-guard POSITIVO.** Para leer un campo `unknown` (ej. `idempotency_key` del request) y asignarlo ya estrechado a `string`, hay que usar la forma **positiva** `if (typeof x === 'string' && RE.test(x)) { key = x; } else return fail(...)`. La forma negada (`if (typeof x !== 'string' || !RE.test(x)) return fail(...); key = x;`) **no** estrecha `x` a `string` en la asignación posterior bajo `strict`, y rompe el `tsc`. (Patrón aplicado al guard de `idempotency_key` en el handler.)
- **L-C-22 — En PowerShell 5.1, una propiedad de array vacía puede deserializar como `$null`; chequear vacío con un helper defensivo o un proxy invariante, nunca con `.Count` pelado.** `ConvertFrom-Json` en PS 5.1 puede devolver `$null` (no `@()`) para una propiedad JSON `[]`, y `@($null).Count == 1` — un chequeo `@($d.arr).Count -eq 0` daría falso negativo. Patrones seguros: (a) un helper `Filas`-style `if ($x) { @($x) } else { @() }` (que `$null` y `@()` colapsan a Count 0), o (b) un **proxy invariante**: como `gastos_internos.monto > 0` por constraint, `por_clase` vacío ⟺ `SumMonto(por_clase) == 0`. Generaliza el helper `Filas` de A25 a principio explícito para todo smoke de lectura con arrays opcionalmente vacíos.
- **L-C-23 — Para un campo híbrido (omitido vs explícito), el gateway espeja la verdad del wrapper DIRECTO de ESA acción, no la de un validador hermano; y nunca es más estricto que su wrapper.** A13 y A25 comparten el andamiaje de período, pero su semántica de `periodo_hasta:null` **difiere legítimamente**: el wrapper directo de A13 trata `null` como omitido (laxo), mientras A25 lo rechaza (estricto). Espejar A25 en A13 habría hecho al gateway **más estricto que su propio wrapper** → divergencia gateway↔directo en un caso de borde. **Regla:** para cada acción, el gateway se valida contra **SU** wrapper directo, no contra hermanos de estructura similar. **Técnica de verificación:** un **test de paridad ejecutable** que porta ambas normalizaciones (gateway-then-wrapper vs wrapper-directo) y compara el resultado sobre una batería de inputs (acá 24/24) — barato y atrapa divergencias sutiles que el razonamiento manual pasa por alto (acá detectó el delta del `null` antes del deploy).

---

## 6. Evidencia (smokes en TEST)

### 6.1 A11 `cargar.gasto_interno`
- **DDL infra + función** (un run transaccional): **8/8 PASS** — `portal_idempotencia` (PK + `uq_nonce` + `uq_action_key` + FK RESTRICT a `gastos_internos` + 7 CHECK) y `portal_cargar_gasto_interno` (DROP+CREATE, L-9F-03).
- **Directo** (`C_SLICE3B_A11_smoke_directo.ps1`, RunID `20260622095134-f901e8`): **38/38 PASS** — seguridad **26/0 escrituras** (`secWrites=0` empírico: HMAC sobre bytes + `ts` ±300 s + rol + action binding + `ambiente` + 8 coherencias de constraint rechazadas sin escribir), funcional 5 altas A/C/D/E con el ciclo replay/retry/doble-click/conflictos exactos (F5b replay byte-idéntico → `nonce_replay`; F5e monto distinto → `payload_mismatch`; F5f actor distinto → `actor_mismatch`), META allowlist.
- **Gateway** (`C_SLICE3B_A11_GW_smoke.ps1`, JWT, runid `20260622114732970`): **24/24 PASS** — 9 seguridad/transporte + 7 spoof de control **en** payload + 3 spoof de control **top-level** + 4 funcional (alta nueva → `id_gasto`; retry idempotente → mismo `id`; `payload_mismatch`/`actor_mismatch` → `conflicto`, **NUNCA** `estado_incierto`; valida `injectActor`) + META. Estático previo: validador 52/52, envelope byte-identidad 9/9, esbuild + `tsc --strict`, diff aditivo, CATALOG 11→12.
- **Teardown** (FK-safe, ambos caminos): **PASS** — `trazas_smoke_restantes=0`, `fixture_9f_intacto=5` (no toca el fixture 9F `creado_por='seed_9f_validacion'`).

### 6.2 A13 `gastos.listado`
- **Directo** (`C_SLICE3B_A13_smoke_directo.ps1`): **32/32 PASS** — seguridad 8 + funcionales 10 (con **particiones data-independent** que cierran contra el universo) + payload inválido 13 (incluye los filtros nuevos) + META. **Lectura → sin teardown.**
- **Gateway** (`C_SLICE3B_A13_GW_smoke.ps1`, JWT): **34/34 PASS** — seguridad 5 + funcionales 12 (cruce al centavo G0a/G0b + cuadre + enum + clamp + particiones + paginación) + **paridad 2** (GP mismo-mes → agosto, NO `payload_invalido`; P-null `null==omitido`) + payload inválido 14 (incluye P4b otro tipo) + META. Estático previo: esbuild rc=0, diff aditivo **111/0**, **paridad ejecutable gateway↔wrapper 24/24**, CATALOG 12→13.
- **Cruce AL CENTAVO** (`C_SLICE3B_A13_smoke_expected.sql`, ventana `[2026-07-01 .. 2099-12-01]`, **idéntico en directo y gateway**): universo `total_gastos=335000.00`, `n_filas=5`, `por_clase` A=40000(1)/C=25000(1)/D=30000(1)/E=240000(2); particiones `A+C+D+E == socio+caja == 335000` (clase / pagador); agosto `180000` (1 fila, usado por la paridad GP de D-C-60).

En las cuatro corridas, único conjunto de códigos de error visto = el del molde de seguridad + `payload_invalido` (+ `conflicto` en A11), **todos dentro de la allowlist**. **Cero `estado_incierto` y cero `error_interno`** en todos los caminos.

---

## 7. Supuestos y límites del slice

- Slice 3b = **gasto interno** (A11 escritura + A13 lectura). La **contabilidad societaria** (lectura A14–A18, escritura A19–A23) queda **fuera** (post-MVP, solo rol socio).
- **No promovido a OPS.** Toda la validación fue en TEST.
- **`portal_idempotencia` + `portal_cargar_gasto_interno` son infra Carril C TEST-only, fuera del canónico** (precedente `portal_usuarios`, D-C-34). `gastos_internos` se usa tal cual (sin `source_event`, sin DDL). A13 es `SELECT` inline.
- **`nonce_replay` no es alcanzable de forma representativa por gateway** (cada request del frontend re-firma → `ts`/firma/`nonce` distintos): queda cubierto por el **smoke directo** (re-POST byte-idéntico). Los mismatches de negocio (`payload_mismatch`/`actor_mismatch`) sí se prueban end-to-end por gateway. (L-C-20.)
- **A13 fetchea todas las filas del período** para los agregados y pagina `filas` en memoria; aceptable para 5 cabañas con período acotado por el floor.
- **Datos de TEST con `periodo` 2026 (jul/ago/nov)**: por eso hoy (junio 2026) el default `{}` de A13 da vacío y el smoke usa `periodo_hasta=2099-12-01` para los casos con datos.

---

## 8. Pendientes y handoff

- **P-C-9 (store anti-replay de `nonce`) → ✅ RESUELTO EN TEST para Carril C** por `portal_idempotencia` (`UNIQUE(nonce)` anti-replay + `UNIQUE(action,idempotency_key)` idempotencia; `nonce` = firma HMAC del sobre, D-C-55/56). **No queda gap de diseño;** su aplicación en OPS viaja dentro de la **promoción coordinada** del Carril C.
- **Hardening pre-OPS restante** (P-C-7/8/10/11, sigue abierto): CORS al origin del portal; `VITA_HMAC_SECRET` de OPS distinto del de TEST; rol Postgres dedicado de mínimos; migrar `portal-api` a CLI + `config.toml` si los redeploys se vuelven frecuentes.
- **Contabilidad societaria A14–A18 / A19–A23** (post-MVP, solo rol socio): fuera del Carril C MVP.
- **Promoción coordinada del Carril C a OPS** — cuando cierren todas las slices del MVP (bump único del canónico; ahí viajan `portal_usuarios`, `portal_idempotencia` y `portal_cargar_gasto_interno`).
- **Frentes paralelos** (Mercado Pago, Marketing): no se cruzan con este cierre.

---

## 9. Deltas para documentos satélite (aplicar tras auditar este cierre)

**EOL por archivo** (verificar contra los archivos reales antes de pegar):

- **`DECISIONES_NO_REABRIR.md`** (LF) — agregar subsección **"## Carril C — Slice 3b (gasto interno: A11 carga + A13 listado) — cerrada en TEST 2026-06-22"** con **D-C-55 / D-C-56 / D-C-57 / D-C-58 / D-C-59 / D-C-60**, **después** del bullet D-C-54 (fin de la sección Slice 3a) y **antes** de "## Lecciones operativas n8n consolidadas (L-6C-XX)". Intro con **CATALOG 11 → 13** (1 Edge + 8 lecturas + 4 escrituras) + cierres. **D-C-54 NO se modifica** (la divergencia A25↔A13 de `periodo_hasta:null` queda documentada solo en D-C-60/L-C-23/este cierre).
- **`Lecciones_Aprendidas.md`** (bloque L-C en **CRLF**) — agregar **L-C-20 / L-C-21 / L-C-22 / L-C-23** después de L-C-19, EOL **CRLF**.
- **`ESTADO_ACTUAL_VITA_DELTA.md`** (CRLF) — nuevo bloque **"Etapa actual: Slice 3b (gastos A11 carga + A13 listado) — cerrada en TEST 2026-06-22"**; el bloque de **Slice 3a** baja de "Etapa actual" a "Etapa previa". CATALOG pasa de 11 a **13** entradas (1 Edge + 8 lecturas + 4 escrituras).
- **`CLAUDE.md`** (LF) — entrada de cierre de **Slice 3b** después de la de Slice 3a, mismo formato; CATALOG 13; D-C-55…60 / L-C-20…23.
- **`Pendiente_pre_produccion.md`** (LF) — **P-C-9** marcado: `✅ RESUELTO EN TEST para Carril C. No queda gap de diseño; su aplicación en OPS viaja dentro de la promoción coordinada.` (+ ref `portal_idempotencia` / D-C-55/56). **Resto P-C-7/8/10/11 sin cambios.**
- **`README.md`** (LF) — actualizar el párrafo de estado (fecha → 2026-06-22) y el bullet del Carril C: agregar **Slice 3b** (gasto interno A11/A13, TEST), **CATALOG 13**, "Próximo" → promoción coordinada del Carril C a OPS.
- **`6B_SCHEMA_SQL.md`** y **OPS** — **sin cambios** (Carril C no toca el canónico; A11 no introduce DDL canónico —la infra de idempotencia vive fuera—, A13 es `SELECT` inline).
- **Commit:** `C_SLICE3B_A13_portal-api_index.ts` (gateway final del slice: A11 + A13 + todo lo previo, CATALOG 13) + los wrappers/smokes/teardowns/expected/runsheets/mini-cierres de A11 y A13 (directo + gateway), sanitizados + este cierre.

---

## 10. Inventario de artefactos de la etapa

- **Snapshot:** `C_SLICE3B_SNAPSHOT_A11_A13.sql` (anti-OPS de `gastos_internos`, validado limpio).
- **A11:** wrapper `portal-a11-cargar-gasto-interno__TEMPLATE.json` + smokes `C_SLICE3B_A11_smoke_directo.ps1` / `C_SLICE3B_A11_GW_smoke.ps1` + teardowns `C_SLICE3B_A11_teardown.sql` / `C_SLICE3B_A11_GW_teardown.sql` + mini-cierres `C_SLICE3B_A11_DIRECTO_CIERRE.md` / `C_SLICE3B_A11_GW_CIERRE.md`. Infra: `portal_idempotencia` + `portal_cargar_gasto_interno` (TEST-only, fuera del canónico).
- **A13:** wrapper `portal-a13-gastos-listado__TEMPLATE.json` + ground-truth `C_SLICE3B_A13_smoke_expected.sql` + smokes `C_SLICE3B_A13_smoke_directo.ps1` / `C_SLICE3B_A13_GW_smoke.ps1` + mini-cierres `C_SLICE3B_A13_DIRECTO_CIERRE.md` / `C_SLICE3B_A13_GW_CIERRE.md`.
- **Gateway final del slice:** `C_SLICE3B_A13_portal-api_index.ts` (CATALOG 13 entradas).
- **Cierre:** este documento (`C_SLICE3B_CIERRE.md`).
